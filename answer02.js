// Q2: Generate Numbers Between Two Given Numbers

function generateNumbers(num1, num2) {
    for (let i = num1 + 1; i <= num2; i++) {
        console.log(i);
    }
}

// Example usage:
generateNumbers(10, 25);

// Output: 11, 12, ..., 25